# Core v1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship two portable Skills — `ground-rules` and `actor-template` — installable natively on Claude Code (via plugin marketplace) and Claude.ai (via Skills upload), with zero custom build steps.

**Architecture:** One Claude Code plugin called `core` lives at `/core` in this repo, indexed by a single marketplace at `.claude-plugin/marketplace.json`. The same `core/skills/<name>/` folders are also the units zipped for Claude.ai upload — one source, two native installs.

**Tech Stack:** Markdown (`SKILL.md`), JSON (`plugin.json`, `marketplace.json`), shell (`zip`, `git`, `gh`), Claude Code (`/plugin`, `/help`, `/reload-plugins`), Claude.ai (Settings → Features → Custom Skills).

**Source spec:** [`../specs/2026-05-20-core-v1-design.md`](../specs/2026-05-20-core-v1-design.md) — every task below traces to a spec section.

---

## File map (what's created or modified)

| Path | Status | Responsibility |
| --- | --- | --- |
| `.claude-plugin/marketplace.json` | Create | Marketplace catalog listing the `core` plugin |
| `core/.claude-plugin/plugin.json` | Create | Plugin manifest (name = namespace, version, description) |
| `core/skills/ground-rules/SKILL.md` | Create | The four discipline rules, with sharpened description + footer |
| `core/skills/actor-template/SKILL.md` | Create | Goal/Input/Steps/Output/Constraints shape, with one worked example + copy-paste template |
| `core/README.md` | Create | Install paths for Claude Code + Claude.ai |
| `core/VERIFICATION.md` | Create | V1/V2/V3 probes + the ~90% threshold |
| `vision/SKILL.md` | Leave as-is | Stays as the original design draft (spec §3) |

No source code, no tests in the traditional sense. The "tests" are the V1/V2/V3 probes from spec §9 — manual, fact-driven, run against real Claude Code and Claude.ai sessions.

---

## Task order rationale (read once, then execute top-to-bottom)

Spec §9 mandates **V1 runs first, not last**. So the order is:

1. Manifests + stub skills (Tasks 1–4) → just enough surface to install.
2. **V1 probe** (Task 5) → catch broken install before writing any real content.
3. Fill in real content (Tasks 6, 8) and verify each triggers (V2 — Tasks 7, 9).
4. Supporting docs (Tasks 10, 11).
5. **V3 probe** (Task 12) → confirm behavior actually changes.
6. Claude.ai install + V1+V2 on App (Tasks 13, 14).
7. Push to GitHub for the public install URL (Task 15).

---

### Task 1 — Create the marketplace manifest

**Inputs:** repo root has no `.claude-plugin/` directory.
**Outputs:** `.claude-plugin/marketplace.json` exists; `jq` parses it; `name` field is `nz-vision`.
**Files:** Create `.claude-plugin/marketplace.json`.
**Verification:** `jq -r .name .claude-plugin/marketplace.json` prints `nz-vision`.
**Spec trace:** §5.2.

- [ ] **1. Create the directory.** Run from repo root: `mkdir -p .claude-plugin`. Expected output: none (silent success).

- [ ] **2. Write the manifest.** Create `.claude-plugin/marketplace.json` with exactly this content:

```json
{
  "name": "nz-vision",
  "owner": { "name": "Nikita Zverev" },
  "description": "Vision — a personal, model-agnostic agentic engineering system. Domain-neutral core, on-demand packs.",
  "plugins": [
    {
      "name": "core",
      "source": "./core",
      "description": "Domain-neutral discipline: ground-rules + actor-template."
    }
  ]
}
```

- [ ] **3. Verify the JSON parses and `name` resolves.** Run: `jq -r .name .claude-plugin/marketplace.json`. Expected output: `nz-vision`.

- [ ] **4. Commit.** Run:

```bash
git add .claude-plugin/marketplace.json
git commit -m "feat(marketplace): add nz-vision marketplace manifest"
```

---

### Task 2 — Create the plugin manifest

**Inputs:** Task 1 complete. `core/` does not exist.
**Outputs:** `core/.claude-plugin/plugin.json` exists; `jq` parses it; `name` is `core`; `version` is `0.1.0`.
**Files:** Create `core/.claude-plugin/plugin.json`.
**Verification:** `jq -r '.name + " " + .version' core/.claude-plugin/plugin.json` prints `core 0.1.0`.
**Spec trace:** §5.1. Docs trace: `code.claude.com/docs/en/plugins` — "Only `plugin.json` goes inside `.claude-plugin/`."

- [ ] **1. Create the directory.** Run: `mkdir -p core/.claude-plugin`. Expected: silent.

- [ ] **2. Write the manifest.** Create `core/.claude-plugin/plugin.json` with exactly this content:

```json
{
  "name": "core",
  "description": "Domain-neutral discipline for trustworthy output: fact-grounding (sources + quotes), no fake-confidence hedging, read-before-write, only-required-output, and the Goal/Input/Steps/Output/Constraints shape for any actor (skill, slash command, or workflow).",
  "version": "0.1.0",
  "author": { "name": "Nikita Zverev" }
}
```

- [ ] **3. Verify.** Run: `jq -r '.name + " " + .version' core/.claude-plugin/plugin.json`. Expected output: `core 0.1.0`.

- [ ] **4. Commit.** Run:

```bash
git add core/.claude-plugin/plugin.json
git commit -m "feat(core): add plugin manifest at v0.1.0"
```

---

### Task 3 — Add the `ground-rules` skill stub

**Inputs:** Task 2 complete. `core/skills/` does not exist.
**Outputs:** `core/skills/ground-rules/SKILL.md` exists with frontmatter only and a one-line stub body.
**Files:** Create `core/skills/ground-rules/SKILL.md`.
**Verification:** the file's first character is `-` (YAML frontmatter), and the file contains `name: ground-rules`.
**Rationale:** spec §9 — "Run V1 first" requires the skill to be present (so `/help` lists it) but the body is irrelevant for the install test. Stub now, fill in Task 6.
**Spec trace:** §4.1 (frontmatter only at this point).

- [ ] **1. Create the directory.** Run: `mkdir -p core/skills/ground-rules`. Expected: silent.

- [ ] **2. Write the stub.** Create `core/skills/ground-rules/SKILL.md` with exactly this content:

```markdown
---
name: ground-rules
description: Apply on every substantive task — answering questions, drafting, research, analysis, planning, coding, reviewing — whenever the response contains any factual claim or could pad/overreach. Enforces four rules: fact-grounding (sources + quotes), no fake-confidence hedging, read the real source before answering, deliver only what was asked.
---

STUB — full body lands in Task 6.
```

- [ ] **3. Verify.** Run: `head -2 core/skills/ground-rules/SKILL.md`. Expected output:

```
---
name: ground-rules
```

- [ ] **4. Commit.** Run:

```bash
git add core/skills/ground-rules/SKILL.md
git commit -m "feat(core): add ground-rules skill stub (frontmatter only)"
```

---

### Task 4 — Add the `actor-template` skill stub

**Inputs:** Task 3 complete.
**Outputs:** `core/skills/actor-template/SKILL.md` exists with frontmatter and a one-line stub body.
**Files:** Create `core/skills/actor-template/SKILL.md`.
**Verification:** the file contains `name: actor-template`.
**Spec trace:** §4.2 (frontmatter only at this point).

- [ ] **1. Create the directory.** Run: `mkdir -p core/skills/actor-template`. Expected: silent.

- [ ] **2. Write the stub.** Create `core/skills/actor-template/SKILL.md` with exactly this content:

```markdown
---
name: actor-template
description: Use when authoring or editing an actor — a Skill, slash command, or workflow that prescribes how to act (not just what is true). Enforces the Goal/Input/Steps/Output/Constraints shape, separates Knowledge from Actor, and demands every step produce an observable result.
---

STUB — full body lands in Task 8.
```

- [ ] **3. Verify.** Run: `head -2 core/skills/actor-template/SKILL.md`. Expected output:

```
---
name: actor-template
```

- [ ] **4. Commit.** Run:

```bash
git add core/skills/actor-template/SKILL.md
git commit -m "feat(core): add actor-template skill stub (frontmatter only)"
```

---

### Task 5 — V1 (Claude Code, local install) — MANUAL PROBE

**Inputs:** Tasks 1–4 complete. Both stub skills present; both manifests present.
**Outputs:** A written probe result (success or specific failure message). If failure, the next steps stop and the manifest is debugged before continuing.
**Files:** none.
**Verification:** the user pastes the observed output back into this plan or the executing session.
**Spec trace:** §9 V1; the "Run V1 first" guardrail.

This task **must be run by the human in a real Claude Code session.** No automation substitutes.

- [ ] **1. From the repo root, open Claude Code.** Run: `claude --plugin-dir ./core`. Expected: Claude Code starts and prints no plugin-load errors. Alternatively (to test the marketplace flow): `/plugin marketplace add .` then `/plugin install core@nz-vision`.

- [ ] **2. Confirm both skills appear under the `core:` namespace.** Type `/help` inside the session. Expected: `/core:ground-rules` and `/core:actor-template` both listed.

- [ ] **3. Record the outcome.** If both skills appear: write `V1 (Code, local): PASS` in your session notes and proceed to Task 6. If anything fails: stop, debug the manifest (most common cause: a JSON syntax error in `plugin.json` or `marketplace.json`), and re-run this task.

---

### Task 6 — Migrate `ground-rules` body from `vision/SKILL.md`

**Inputs:** Task 5 returned PASS. `vision/SKILL.md` contains the four-rule draft.
**Outputs:** `core/skills/ground-rules/SKILL.md` contains the tightened frontmatter from Task 3 plus the full four-rule body and a one-line footer.
**Files:** Modify `core/skills/ground-rules/SKILL.md`.
**Verification:** `grep -c '^## ' core/skills/ground-rules/SKILL.md` prints `5` (four rule headings + the closing "What this skill never does" heading). `grep -c "actor-template" core/skills/ground-rules/SKILL.md` prints `1` (the footer).
**Spec trace:** §4.1.

- [ ] **1. Replace the file contents.** Overwrite `core/skills/ground-rules/SKILL.md` with exactly:

```markdown
---
name: ground-rules
description: Apply on every substantive task — answering questions, drafting, research, analysis, planning, coding, reviewing — whenever the response contains any factual claim or could pad/overreach. Enforces four rules: fact-grounding (sources + quotes), no fake-confidence hedging, read the real source before answering, deliver only what was asked.
---

# Ground Rules

These four rules make output trustworthy. They apply together, on every substantive task, regardless of domain. They are the reason to trust the result.

## 1. Fact-grounding — every factual claim carries a source

A factual claim is any statement presented as true about the world, a document, a codebase, or data. Every such claim must come with:
- a **source** — a document, URL, file path, dataset, or the user's own confirmed statement, and
- a **searchable quote** — a short verbatim snippet the reader can locate in the source in seconds.

If a claim has no source: do not state it as fact. Either drop it, or mark it explicitly:
- `[assumption: <why>]` — a reasonable inference, labelled as such.
- `[cannot verify]` — relevant but unconfirmable.

This applies to **facts only**. Opinions, suggestions, and creative drafting are not claims and need no source — but they must be owned as opinion, not smuggled in as fact (see rule 2).

**Example**
- Weak: "This library handles retries automatically."
- Grounded: "This library retries failed requests — `docs/client.md`: \"requests retry up to 3 times by default.\""
- Honest fallback: "`[cannot verify]` whether retries are automatic — the docs don't say; I'd check the source before relying on it."

## 2. No fake confidence, no disguised facts

Banned: stating something uncertain as if certain, and using vague qualifiers ("probably", "typically", "usually", "I assume", "based on convention") to *dodge* sourcing a fact.

The test: if a hedge word is hiding a fact you should have sourced → remove it and source the fact, or mark `[assumption]`. If it's honest opinion or genuine uncertainty stated *as* such → it's fine and natural.

**Example**
- Blocked: "This is probably the fastest option." (a fact-claim with no source, hidden behind "probably")
- Allowed: "I'd lean toward this option, but it's your call." (clearly an opinion, owned as one)
- Allowed: "I'm not certain which is faster — `[cannot verify]` without a benchmark."

Never use "typically" / "I assume" / "based on convention" *in place of* actually checking.

## 3. Read the real source before answering

Before answering anything checkable, look in this order and stop at the first that answers it:
1. What you already reliably know.
2. Documents and files the user has provided.
3. Trusted external sources — search the web when the answer could have changed since training, or when unsure.
4. Ask the user — only after the above are exhausted.

Do not guess when you can check. If you genuinely cannot check, say so plainly rather than filling the gap with a confident guess.

## 4. Deliver only what was asked — no scope creep

Deliver exactly the task. No padding, no unrequested extras, no "while I was at it I also changed…".
- If something adjacent looks worth doing, mention it in one line and let the user decide — don't just do it.
- Shorter is the default. Length is earned by the task, not by habit.

**Example**
- Asked: "Fix the typo in the heading."
- Right: fix the typo. If you notice a broken link nearby: "Done. (Unrelated: the link in the next line looks broken — want me to fix that too?)"
- Wrong: fix the typo *and* silently rewrite the paragraph, reformat the page, and "improve" the tone.

## What this skill never does

- State a fact without a source, or hide an unsourced fact behind a vague word.
- Answer from memory when the answer could have changed and checking was possible.
- Pad output or do unrequested work.
- Pretend to a certainty it doesn't have.

---

Writing or editing an actor (Skill, slash command, workflow)? See `actor-template`.
```

- [ ] **2. Verify section count.** Run: `grep -c '^## ' core/skills/ground-rules/SKILL.md`. Expected output: `5`.

- [ ] **3. Verify footer.** Run: `grep -c "actor-template" core/skills/ground-rules/SKILL.md`. Expected output: `1`.

- [ ] **4. Commit.** Run:

```bash
git add core/skills/ground-rules/SKILL.md
git commit -m "feat(core): land full ground-rules body + actor-template footer"
```

---

### Task 7 — V2 for ground-rules — MANUAL PROBE

**Inputs:** Task 6 complete.
**Outputs:** Documented outcome: PASS or specific failure note.
**Files:** none.
**Verification:** the user observes ground-rules activating on an ungrounded claim in a fresh Claude Code session.
**Spec trace:** §9 V2.

- [ ] **1. Reload plugins.** In the running Claude Code session, run `/reload-plugins`. Expected: status line confirms reload.

- [ ] **2. Probe with an ungrounded claim.** Type: `Tell me whether library X handles retries automatically.` Expected: the response either (a) sources the answer with a quote, (b) marks `[cannot verify]`, or (c) explicitly invokes `ground-rules`. Any of those three is PASS.

- [ ] **3. Record outcome.** Write `V2 (Code, ground-rules): PASS` or note the failure mode (e.g., "responded with `probably yes` and no source — description didn't trigger"). On failure, **do not yet edit the description** — see spec §11 follow-up.

---

### Task 8 — Write `actor-template` body

**Inputs:** Task 7 returned PASS or has a documented failure that is not blocking.
**Outputs:** `core/skills/actor-template/SKILL.md` contains the full body per spec §4.2 (six sections, one worked example, one copy-paste template, footer).
**Files:** Modify `core/skills/actor-template/SKILL.md`.
**Verification:** `grep -c '^## ' core/skills/actor-template/SKILL.md` prints `5` (five `##` sections). `grep -c "Observable result" core/skills/actor-template/SKILL.md` prints `>= 3` (worked example contains observable results).
**Spec trace:** §4.2.

- [ ] **1. Replace the file contents.** Overwrite `core/skills/actor-template/SKILL.md` with exactly:

````markdown
---
name: actor-template
description: Use when authoring or editing an actor — a Skill, slash command, or workflow that prescribes how to act (not just what is true). Enforces the Goal/Input/Steps/Output/Constraints shape, separates Knowledge from Actor, and demands every step produce an observable result.
---

# Actor Template

An Actor prescribes *how to act* (the Goal/Input/Steps/Output/Constraints shape below). It is distinct from Knowledge, which encodes *what is true* (rules, patterns, references). The two never mix in one file — see `ground-rules` for the truth half.

## The five required sections

Every Actor file must contain exactly these five sections, in this order, no renames, no merges:

1. **Goal** — what success looks like, in one sentence. Failure mode without it: the actor wanders.
2. **Input** — what the actor needs from the caller. Failure mode without it: the actor invents data.
3. **Steps** — numbered, in order. Each Step produces an *observable result* (a written file, a returned value, a confirmed message). Failure mode without it: a Step without observable result hides ambiguity.
4. **Output** — what the actor returns when done. Failure mode without it: callers cannot tell success from in-progress.
5. **Constraints** — what the actor must not do; the boundary. Failure mode without it: the actor over-reaches.

## Rules

- **Every Step produces an observable result.** No "consider", "think about", "review" without an artifact.
- **No Knowledge bleed.** If the body would contain a rule, pattern, or reference table, that material belongs in a Knowledge skill — not here.
- **No section combined or renamed.** "Goal & Input" or "Procedure" is a defect.

## One worked example — Summarize a pull request

**Goal:** Produce a 3-bullet summary of a pull request so a reviewer can decide whether to dig deeper in under 30 seconds.

**Input:** The PR number; the repository (`owner/name`).

**Steps:**
1. Fetch the PR title, description, and changed-files list via `gh pr view <number> --repo <owner/name>`. Observable result: the title and description are quoted verbatim in the working scratchpad.
2. Identify the largest changed file by diff size via `gh pr diff <number> --repo <owner/name> | diffstat`. Observable result: filename + diff-line count noted.
3. Write three bullets: (a) what the PR claims to do, (b) the largest mechanical change, (c) any test changes. Observable result: three bullets written to the output, each ≤ 1 line.

**Output:** Three bullets, ≤ 1 line each, prefixed with the PR number (e.g., `#123:`).

**Constraints:** Do not approve, merge, or comment on the PR. Do not summarize PRs in draft state — return "PR is in draft" instead.

## Copy-paste template

```
---
name: <kebab-case-name>
description: <one-paragraph trigger: when to use this actor; the specific verbs and nouns that should activate it>
---

# <Title>

## Goal
<one sentence>

## Input
- <item>
- <item>

## Steps
1. <action>. Observable result: <artifact>.
2. <action>. Observable result: <artifact>.

## Output
<what is returned>

## Constraints
- <hard limit>
- <hard limit>
```

## What this skill never does

- Mix Knowledge (rules, patterns, references) into an Actor file.
- Allow a Step without an observable result.
- Rename or merge the five required sections.

---

Every output an actor produces must still obey `ground-rules`.
````

- [ ] **2. Verify section count.** Run: `grep -c '^## ' core/skills/actor-template/SKILL.md`. Expected output: `5`.

- [ ] **3. Verify observable-result rule appears in the worked example.** Run: `grep -c "Observable result" core/skills/actor-template/SKILL.md`. Expected: `>= 3`.

- [ ] **4. Commit.** Run:

```bash
git add core/skills/actor-template/SKILL.md
git commit -m "feat(core): land actor-template body with PR-summary worked example"
```

---

### Task 9 — V2 for actor-template — MANUAL PROBE

**Inputs:** Task 8 complete.
**Outputs:** Documented outcome: PASS or specific failure.
**Files:** none.
**Spec trace:** §9 V2.

- [ ] **1. Reload plugins.** In Claude Code: `/reload-plugins`.

- [ ] **2. Probe with an authoring request.** Type: `Help me create a new skill that posts daily standup summaries to Slack.` Expected: the response uses the Goal / Input / Steps / Output / Constraints shape, or explicitly invokes `actor-template`. Either is PASS.

- [ ] **3. Record outcome.** Write `V2 (Code, actor-template): PASS` or note the failure mode.

---

### Task 10 — Create `core/README.md`

**Inputs:** Tasks 1–9 complete.
**Outputs:** `core/README.md` exists with three sections (What's here, Install on Claude Code, Install on Claude.ai).
**Files:** Create `core/README.md`.
**Verification:** the file has exactly three top-level (`## `) sections. `grep -c "^## " core/README.md` prints `3`.
**Spec trace:** §8.

- [ ] **1. Write the file.** Create `core/README.md` with exactly this content:

```markdown
# Core

Two domain-neutral discipline skills that make output trustworthy and any actor (skill / slash command / workflow) follow a single, observable shape. For the "why" behind this package, see [`../vision/VISION.md`](../vision/VISION.md).

## What's here

- **`ground-rules`** — Apply on every substantive task. Enforces: every factual claim carries a source + quote; no fake-confidence hedging; read the real source before answering; deliver only what was asked.
- **`actor-template`** — Apply when authoring or editing a Skill, slash command, or workflow. Enforces the Goal / Input / Steps / Output / Constraints shape and demands every Step produce an observable result.

## Install on Claude Code

```
/plugin marketplace add NVZver/claude-marketplace
/plugin install core@nz-vision
/reload-plugins
```

Invoke directly via `/core:ground-rules` or `/core:actor-template`, or let Claude trigger them automatically by description match. Run `/reload-plugins` after editing skill files to pick up changes without restart.

## Install on Claude.ai

Each skill uploads separately (Claude.ai's native model). From the repo root:

```bash
cd core/skills && zip -r ground-rules.zip ground-rules/ && zip -r actor-template.zip actor-template/
```

Then in Claude.ai → **Settings → Features → Custom Skills**, upload `ground-rules.zip` and `actor-template.zip` separately.

Per Anthropic docs (`platform.claude.com/docs/en/agents-and-tools/agent-skills/overview`): *"Custom Skills do not sync across surfaces. Skills uploaded to one surface are not automatically available on others."* The upload is one-time, per user; there is no organization-wide distribution path on Claude.ai today.
```

- [ ] **2. Verify section count.** Run: `grep -c '^## ' core/README.md`. Expected: `3`.

- [ ] **3. Commit.** Run:

```bash
git add core/README.md
git commit -m "docs(core): add README with install paths for Code and App"
```

---

### Task 11 — Create `core/VERIFICATION.md`

**Inputs:** Task 10 complete.
**Outputs:** `core/VERIFICATION.md` exists with V1, V2, V3 probes and the ~90% threshold.
**Files:** Create `core/VERIFICATION.md`.
**Verification:** `grep -c "^## V" core/VERIFICATION.md` prints `3`.
**Spec trace:** §9.

- [ ] **1. Write the file.** Create `core/VERIFICATION.md` with exactly this content:

```markdown
# Core v1 — Verification

Three manual probes. Run them on a fresh session of each surface. No automated harness in v1 — see spec §6 (deferred eval rigor).

## V1 — Installs cleanly on both surfaces

**Claude Code.** Run:
```
/plugin marketplace add NVZver/claude-marketplace
/plugin install core@nz-vision
/help
```
Expected: `/help` lists `/core:ground-rules` and `/core:actor-template` with their full descriptions.

**Claude.ai.** From repo root: `cd core/skills && zip -r ground-rules.zip ground-rules/ && zip -r actor-template.zip actor-template/`. Then upload each zip in Settings → Features → Custom Skills. Expected: both appear in the Skills list with their descriptions visible.

## V2 — Description-match triggers reliably

**Probe A (ground-rules).** In a fresh session: *"Tell me whether library X handles retries automatically."* Expected: the response sources the answer with a quote, marks `[cannot verify]`, or explicitly invokes `ground-rules`. Any of those is PASS.

**Probe B (actor-template).** In a fresh session: *"Help me create a new skill that posts daily standup summaries to Slack."* Expected: the response uses the Goal / Input / Steps / Output / Constraints shape, or explicitly invokes `actor-template`. Either is PASS.

Run both probes on **both** surfaces.

## V3 — Behavior change is observable

Run the same small task twice — once with `core` installed, once without — and compare:

- **Accuracy to task.** Did the output do what was asked, no more, no less?
- **Proven facts with sources.** Share of agent claims carrying a valid source + searchable quote.
- **Only-required-changes.** Did the change touch only what the task needed?

Eyeball it for v1; instrument later (spec §6 adjust #3).

## Falsifiable threshold

Across two weeks of regular use, log every session where `ground-rules` *should* have fired (any factual claim was made or any output overreached). If it fires on fewer than **~90% of intended tasks**, that is a v1 failure mode — not a wording tweak. Revisit the `CLAUDE.md`-fragment option from VISION §3 before tightening the description further.
```

- [ ] **2. Verify the three sections exist.** Run: `grep -c "^## V" core/VERIFICATION.md`. Expected: `3`.

- [ ] **3. Commit.** Run:

```bash
git add core/VERIFICATION.md
git commit -m "docs(core): add VERIFICATION with V1/V2/V3 + 90% threshold"
```

---

### Task 12 — V3 behavior comparison — MANUAL PROBE

**Inputs:** Tasks 1–11 complete.
**Outputs:** Side-by-side notes for one chosen task: output-with-core vs. output-without-core, scored on the three VISION §5 metrics.
**Files:** none (notes live in the user's scratchpad).
**Spec trace:** §9 V3.

- [ ] **1. Pick one small, real task.** Suggestion: ask Claude to explain a function in this repo's `LSA/lsa-specify.md`. The task should be small enough to run twice in 5 minutes total.

- [ ] **2. Run with core installed.** Note: which claims have sources? did output stick to the ask?

- [ ] **3. Disable the plugin** (`/plugin disable core@nz-vision`), then run the same task in a fresh session. Note the same three points.

- [ ] **4. Re-enable.** Run: `/plugin enable core@nz-vision`.

- [ ] **5. Record outcome.** Write three bullets comparing the two runs against the metrics. If with-core is no better than without-core on any metric, that's a v1 failure signal — flag it in the falsifiable-threshold log.

---

### Task 13 — Prepare Claude.ai zips

**Inputs:** Tasks 1–12 complete.
**Outputs:** `ground-rules.zip` and `actor-template.zip` exist at `core/skills/`. Each zip's top-level entry is the skill folder name (not a nested path).
**Files:** Create `core/skills/ground-rules.zip` and `core/skills/actor-template.zip` (these are build artifacts; not committed).
**Verification:** `unzip -l core/skills/ground-rules.zip | head -3` shows `ground-rules/SKILL.md`. Same for actor-template.

- [ ] **1. Run the zip command.**

```bash
cd core/skills && zip -r ground-rules.zip ground-rules/ && zip -r actor-template.zip actor-template/
```

Expected: two new `.zip` files in `core/skills/`.

- [ ] **2. Verify zip layout.** Run: `unzip -l core/skills/ground-rules.zip | grep SKILL.md`. Expected output contains the line: `ground-rules/SKILL.md`.

- [ ] **3. Repeat for actor-template.** Run: `unzip -l core/skills/actor-template.zip | grep SKILL.md`. Expected: `actor-template/SKILL.md`.

- [ ] **4. Add zips to `.gitignore`.** Open `.gitignore` (create if absent). Add these lines:

```
core/skills/*.zip
```

- [ ] **5. Commit the gitignore change only** (not the zips):

```bash
git add .gitignore
git commit -m "chore(core): gitignore generated skill zips"
```

---

### Task 14 — V1+V2 on Claude.ai — MANUAL PROBE

**Inputs:** Task 13 complete.
**Outputs:** Documented outcome for App-side install and triggering.
**Files:** none.
**Spec trace:** §9 V1, V2.

This task **must be run by the human in a real Claude.ai session.** No automation substitutes.

- [ ] **1. Upload both zips.** In Claude.ai: Settings → Features → Custom Skills → Upload. Upload `core/skills/ground-rules.zip`, then `core/skills/actor-template.zip`. Expected: both skills appear in the Skills list with their descriptions.

- [ ] **2. Open a fresh Claude.ai chat.** Run V2 Probe A: *"Tell me whether library X handles retries automatically."* Expected: same PASS conditions as Task 7.

- [ ] **3. Run V2 Probe B in the same fresh chat or a new one:** *"Help me create a new skill that posts daily standup summaries to Slack."* Expected: same PASS conditions as Task 9.

- [ ] **4. Record outcome.** Write `V1 (App): PASS` / `V2 (App, ground-rules): PASS` / `V2 (App, actor-template): PASS` or note the specific failure.

---

### Task 15 — Push to GitHub for the public install URL

**Inputs:** Tasks 1–14 complete with PASS outcomes.
**Outputs:** All commits pushed to `github.com/NVZver/claude-marketplace`. The public install command works end-to-end.
**Files:** none modified; all prior commits pushed.
**Verification:** `git status` shows the local branch is up-to-date with `origin/main`. A `/plugin marketplace add NVZver/claude-marketplace` from a fresh machine resolves and installs.

- [ ] **1. Confirm the remote.** Run: `git remote -v`. Expected: `origin` points to `https://github.com/NVZver/claude-marketplace`.

- [ ] **2. Push.** Run: `git push origin main`. Expected: push succeeds (the gh CLI is logged in as `nikita-zverev-gg` with token scope `repo`; verify push access if rejected).

- [ ] **3. Verify the public install path** (optional but recommended). From a separate machine or after `/plugin marketplace remove nz-vision`, run:

```
/plugin marketplace add NVZver/claude-marketplace
/plugin install core@nz-vision
```

Expected: same V1 PASS conditions as Task 5.

---

## Self-review (executed before publishing this plan)

**Spec coverage.** Every spec section maps to at least one task:

| Spec section | Tasks |
| --- | --- |
| §3 Architecture & layout | 1, 2, 3, 4 |
| §4.1 ground-rules | 3, 6, 7 |
| §4.2 actor-template | 4, 8, 9 |
| §5 Manifests | 1, 2 |
| §6 Install paths | 5, 14, 15 |
| §7 Cross-references | 6, 8 (footer lines) |
| §8 README | 10 |
| §9 Verification (V1/V2/V3) | 5, 7, 9, 12, 14 |
| §9 Falsifiable threshold | 11 |
| §10 Out-of-scope | (none — explicit non-tasks, correctly absent) |
| §11 Follow-ups | (none — post-v1, correctly absent) |

**Placeholder scan.** No "TBD", "TODO", "handle errors appropriately", or similar. All file contents written out verbatim.

**Type/identifier consistency.** Skill names (`ground-rules`, `actor-template`), plugin name (`core`), marketplace name (`nz-vision`), namespace prefix (`/core:`), and repo (`NVZver/claude-marketplace`) used identically across every task.

**Risk surfaces called out.** Task 5 catches install errors before any body content is written (spec §9). Tasks 7, 9, 12, 14 surface description-match failures early — before the user invests in further pack work.
