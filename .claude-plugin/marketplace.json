{
  "name": "NVZver",
  "owner": { "name": "Nikita Zverev" },
  "description": "Vision — a personal, model-agnostic agentic engineering system. Domain-neutral core, on-demand packs.",
  "plugins": [
    {
      "name": "core",
      "source": "./core",
      "description": "Domain-neutral discipline: ground-rules + actor-template."
    },
    {
      "name": "lsa",
      "source": "./lsa",
      "description": "Living Spec Architecture: a technology-agnostic spec layer — skills that discover intent, author a grounded spec (EARS + Gherkin), verify it before build and reconcile the diff after, driven by an orchestrator agent. The implementer (Claude Code, Cursor, a human) is external. Loop: discover → specify → verify → delegate → reconcile."
    },
    {
      "name": "helper",
      "source": "./helper",
      "description": "Friendly fact-grounded assistant: `/help` slash command + auto-engaging subagent on user-friction signals (consecutive discover User Verification rejections, free-form `what is X?` mid-flow), with per-signal-type cooldown to prevent nag. Depends on `core` (output discipline) and `lsa` (User Verification reject signal)."
    },
    {
      "name": "management",
      "source": "./management",
      "description": "Product and project management discipline: shapes vague ideas into structured pitches before the build cycle. Ships a product-manager agent with dynamic domain-role adaptation and a start-feature skill that hands off to management:roadmap on approval. Depends on `core` (ground-rules, output)."
    },
    {
      "name": "prompt-engineer",
      "source": "./prompt-engineer",
      "description": "Prompt engineering discipline: an agent and commands that review, optimize, and create prompts against a shared quality rule set (ground rules, separation of concerns, KISS/DRY, AI sweep, context budget)."
    }
  ]
}
