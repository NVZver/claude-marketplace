> **Trace.** On load, print first: `=============== [.lsa/metrics.md] [vision] ===============`

# Metrics — claude-marketplace

Aggregate one-line row per reconciled cycle (was `T3`). Written by `lsa:reconcile` on a PASS verdict, via `scripts/metrics-harvest.sh` — see `lsa/skills/reconcile/SKILL.md` §Steps step 5. Pass/fail counts only — no statistical eval per `.lsa/VISION.md` §6 *"Adjust #3"*.

Schema: `feature` · `archived` · `accuracy (M/N)` · `Citation resolve-rate (M/N)` · `only-required-changes (M/N)` · `notes`.

The **Citation resolve-rate** column is the `scripts/check-citations.sh` resolve-rate: it proves each `path:line` citation still points at a real line, not that the quoted text is intact (`scripts/check-citations.sh:12-13`). It is a repo-wide **proxy** for "proven facts with sources" — named for exactly what it measures, not for a stronger claim it cannot support.

**Read the Citation resolve-rate column as a repo-level trend, never as a per-cycle score.** It is measured across the whole repo at the moment of harvest, so rows harvested close together carry the same value by construction and the column shows no per-feature variation. A cycle that added no citations and a cycle that added fifty will record identically. The two other columns are genuinely per-cycle; this one is a snapshot of the repo that happens to be stamped on the row.

| Feature | Archived | Accuracy | Citation resolve-rate | Only-required-changes | Notes |
|---|---|---|---|---|---|
| `diagonal-cross-artifact-analysis` | 2026-05-21 | 6/6 (1.00) | 57/60 (~0.95) | 3/3 (1.00) | Pre-contract row (predates the `metrics-harvest.sh`/reconcile-emit-guard schema). First end-to-end LSA dogfood loop on this repo; 13 findings + 2 verify warnings logged in archive; 5 items closed in-feature (Findings #3, #4, #7 + warnings W1, W2); 10 findings deferred (#1, #2, #5, #6, #8–#13). |
| `2026-05-21-ears-journey-shape-ac` | 2026-05-21 | 4/4 (1.00) | ≈68/70 (~0.97) | 5/5 (1.00) | Pre-contract row (predates the `metrics-harvest.sh`/reconcile-emit-guard schema). EARS + journey-shape AC discipline; dual `lsa-verify` trace predicates; epic `**Covers:**` field. Spec dogfoods its own rule (ACs in EARS form, journey-shaped). 8 KISS/DRY/SRP findings caught by mid-loop `/core:ground-rules` audit — all 8 closed in-feature. 1 verify warning (W1: epic-AC wording drift) closed before sync. Loop cost ≈19 questions vs. prior feature's 11 — driven by OQ2 mid-spec scope expansion (F8 add) + ground-rules audit cleanup. |
| `agents-md-canonical` | 2026-07-19 | 14/14 (1.00) | 85/85 (1.00) | 8/8 (1.00) | **Retro-graded 2026-07-20.** `standards-conformance-agents-md` epic 1, PR #77 @ `29a7d85`. Harvested with the explicit cycle range. R13's stated rationale (regenerate `project-map.yaml` for a new root file) rests on a false premise — the map is directories-only — but the freshness check it protects is green. R9's falsification probe is the implementer's claim, not re-run by the grader. |
| `harvest-script` | 2026-07-19 | 13/13 (1.00) | 85/85 (1.00) | 2/2 (1.00) | **Retro-graded 2026-07-20.** `restore-tracked-metrics-harvest` epic 1, PR #78 @ `b26c2b2`. Merged with its verdict left `@ <pending>` — the self-check's promised independent run never happened; supplied retroactively. Two defects found then fixed: this file violated its own epic's canonical orphan-line contract, and `only-required-changes` measured the working tree rather than the cycle (harvested as `65/65` against a real diff of 2 files, inflated by ungitignored `dist/`). |
| `reconcile-emit-guard` | 2026-07-20 | 11/11 (1.00) | 85/85 (1.00) | 9/9 (1.00) | **Retro-graded 2026-07-20.** `restore-tracked-metrics-harvest` epic 2, PR #79 @ `ea3df0c`. The epic that restored this ledger shipped without writing a row to it, and without a `conformance.md` of its own — C17 guards the instruction text, not the emitted artifact, so nothing noticed. Closed by lint C19. R7's falsification test is the strongest artifact in the sweep. |
| `standards-claim` | 2026-07-20 | 13/13 (1.00) | 85/85 (1.00) | 7/7 (1.00) | **Retro-graded 2026-07-20.** `standards-conformance-agents-md` epic 2, PR #80 @ `1307431`. Reference case for premise-checking: the parent pitch asserted 20/20 Agent Skills conformance; the implementer ran the real validator, found 13/20, recorded it honestly and backlogged the fix instead of folding it in. Later corrected to a true 20/20 by PR #84. |
| `format-and-staleness-gate` | 2026-07-20 | 13/13 (1.00) | 85/85 (1.00) | 10/10 (1.00) | **Retro-graded 2026-07-20.** `pinned-library-specs` epic 1, PR #81 @ `502f945`. `libs: {}` empty at this commit as specified; `check-lib-pins.sh` covered by a 6/6 hermetic suite exercising all three exit codes. Version taken as 0.31.0 under R12's next-unused-MINOR rule. |
| `github-actions-pin` | 2026-07-20 | 11/11 (1.00) | 85/85 (1.00) | 3/3 (1.00) | **Retro-graded 2026-07-20.** `pinned-library-specs` epic 2, PR #82 @ `a976926`. Supersedes the pitch's locked dogfood target (Claude Code platform surface), which would have made every `lsa:verify` NOT-GROUNDED forever — an authorised correction, recorded in `roadmap.yaml`. Gate green for a real reason: R8 confirms no suppression mechanism was added. |
| `conditional-read-precedence` | 2026-07-20 | 9/9 (1.00) | 85/85 (1.00) | 3/3 (1.00) | **Retro-graded 2026-07-20.** `pinned-library-specs` epic 3, PR #83 @ `fc01047`. Closes the pitch's sharpest risk (rabbit hole 1, read-order inversion): precedence conditional on the script's exit code, never the model's judgment of freshness, and an unknown explicitly denied a soft pass. |
| `index-query-pipeline` | 2026-08-17 | 7/7 (1.00) | 85/85 (1.00) | 10/10 (1.00) | `rag-context-engine-and-repo-indexing` epic 1 — this repo's first-ever runtime dependency and first-ever containerization. Harvested against the live working tree before commit (no diff-range); since committed as `7a22662` (implementation) + `acf5c4b` (this verdict, separate commit). All 7 requirements verified via live 3/3 scenario runs each, including R5 against a harder condition than the spec required (Docker Desktop left the daemon socket *hung*, not merely absent — `docker info` itself hangs, independently reproduced; both scripts' bounded check correctly reported the fault every time regardless). Two pre-existing spec-format defects caught and fixed at reconcile time — `requirements.md` heading format vs. `coverage-skeleton.sh`'s expected `- Rn.` list items, and a bare-ID column requirement in `metrics-harvest.sh` — zero semantic change to the 7 requirements themselves. |
| `commit-triggered-sync` | 2026-08-17 | 5/5 (1.00) | 89/89 (1.00) | 7/7 (1.00) | `rag-context-engine-and-repo-indexing` epic 2, implementation `d96f334` (harvested with explicit range `249ac48..d96f334`, cycle already committed). Implementer committed directly rather than leaving an uncommitted diff — this verdict still lands in its own separate commit per the independence rule. All 5 requirements verified via independent scratch-repo tests (R2/R3/R4 3/3 each; R1's scoping check is a single deterministic run, no stochastic variance expected). `check-rag-index-matches-head.sh` correctly escalates Docker-unreachable to a hard CI failure (unlike the local `check-rag-index-fresh.sh` gate check's `[cannot verify]` pass-through) — verified live against this sandbox's genuinely-down daemon plus 3x via stub. |
| `discover-verify-wiring` | 2026-08-17 | 7/7 (1.00) | 89/89 (1.00) | 6/6 (1.00) | `rag-context-engine-and-repo-indexing` epic 3, implementation `18a26c2` (harvested with explicit range `1e32ee5..18a26c2`, cycle already committed). Wires `scripts/rag-query.sh` (epic 1) into `discover` Step 1 and `verify` Step 2 (distinct from Step 1's unchanged `resolve-refs.sh` named-symbol resolution). First epic in this initiative to touch `lsa` plugin `artifact_paths` — version bump `0.33.0` → `0.34.0`, CHANGELOG + README updated same-commit. Prose-only change (no executable code): "proving runs" means independent re-reading of the actual diff (not the implementer's quoted excerpts) plus a cold trace-through of both edited steps, not stochastic scenario execution. One nuance recorded rather than hidden: `verify` Step 2's scope-derivation relies on `discover`'s upstream facts rather than re-consulting `project-map.yaml` inline — confirmed pre-existing, not introduced by this epic. |
| `reconcile-wiring` | 2026-08-17 | 4/4 (1.00) | 89/89 (1.00) | 5/5 (1.00) | `rag-context-engine-and-repo-indexing` epic 4 of 4, implementation `957d02b` (harvested with explicit range `06c4d0e..957d02b`) — **last epic of this pitch.** Extends `scripts/rag-query.sh` with a `--sha <sha>` per-path filter (`git diff --quiet <sha> HEAD -- <path>`, no `docker/rag_cli.py` schema change) and wires it into `reconcile` Step 4's semantic-mapping judgment. Safety-critical check: `reconcile`'s Constraints section (independent-grader, never-routed-down, independence-must-be-observable rules) confirmed byte-for-byte unchanged via direct `git diff` inspection, not trusted on the implementer's report — this being the epic that touches the grader's own instructions made that check the highest-priority one in this whole initiative. A first re-verification attempt gave a false negative (reproduced the filter loop inline in the grading session rather than invoking the real script file, hit a sandbox-specific `eval`-wrapping artifact where `git` resolved as "command not found" inside process substitution) — corrected by testing the actual file end-to-end with a stubbed `docker`, which matched the implementer's claimed result exactly. New dependency noted: `jq` (first use in `scripts/`, guarded, only required on the `--sha` path). |
| `path-scoped-query-fix` | 2026-08-18 | 6/6 (1.00) | 89/89 (1.00) | 8/8 (1.00) | `rag-context-engine-and-repo-indexing` epic 5, implementation `2891944` (harvested with explicit range `147ecf1..2891944`) — **discovered via measurement, not pre-planned.** The e2e eval (`.lsa/observations/2026-08-17-rag-eval/`) found `docker/rag_cli.py cmd_query` had no `--path` argument despite epic 3's own shipped prose already claiming "query within resolved scope." Fixed with a real LanceDB pre-filter (`.where(..., prefilter=True)`, evaluated before the ANN top-K) — verified against the actual installed `lancedb==0.25.0` API, not assumed. Independently reproduced the exact regression the eval found (an out-of-scope doc at 0.8842 outranking the correct in-scope script at 0.8697) and confirmed the fix resolves it, by rebuilding the Docker image myself and re-running the query live, not trusting the implementer's report. `lsa` 0.35.0 → 0.36.0. |
| `hybrid-retrieval` | 2026-08-18 | 6/6 (1.00) | 89/89 (1.00) | 1/1 (1.00) | `rag-context-engine-and-repo-indexing` epic 6, implementation `7122606` (harvested with explicit range `b1ebebe..7122606`) — closes the two remaining eval misses (P5, P10) with a real dense+lexical hybrid, not a patch: native LanceDB FTS index + `RRFReranker` fusion, `MIN_SIMILARITY` now an OR-gate (dense floor OR genuine FTS match). `LanceHybridQueryBuilder` itself was tried and rejected — its single shared prefilter flag silently drops valid in-scope FTS matches or truncates before path-filtering, the same failure class the prior epic's own `--path` fix was built to avoid. Verified live: rebuilt the whole-repo index myself, confirmed both fix targets now hit and the P9 regression guard is unaffected. **Self-correction surfaced and independently verified, not smoothed over:** the original eval report's Finding 5 claimed semantic search "correctly avoided" an incidental Redis mention — actually, `.lsa/` (dot-prefixed) was never indexed at all, so that file was never a candidate for either signal. **Larger finding, out of this epic's scope:** `.lsa/` is 9.0 MB, dwarfing every currently-indexed directory combined (≈1 MB) — flagged for a human decision, not fixed here. |
| `index-lsa-content` | 2026-08-18 | 9/9 (1.00) | 89/89 (1.00) | 4/4 (1.00) | `rag-context-engine-and-repo-indexing` epic 7, implementation `7545a4b` (harvested with explicit range `0b0a72c..7545a4b`) — indexes `.lsa/` (specs, pitches, roadmap, research, standards), silently excluded since epic 1 by a blanket dot-directory rule meant only for `.git/`. **Corrected size before building, not after:** 2.4 MB, not the initially-overstated 9.0 MB (which wrongly counted the index's own `.rag-index/` output). Two more gaps found and fixed mid-verification, not pre-planned: `dist/` (gitignored duplicate build output) and `.remember/` (gitignored private session notes) — the `.remember/` fix required a second pass after a genuine Docker build-cache staleness incident (`docker build -q` kept serving a stale image despite a correct source file; `--no-cache` resolved it, recorded as an incident, not silently retried past). Both patches are targeted, not general — the indexer still doesn't consult `.gitignore` itself, flagged for a human decision. Real build-time cost disclosed: 88s → ~2:05 (374 files/2,586 chunks, up from 123/1,199). |
| `index-freshness` | 2026-08-18 | 7/7 (1.00) | 89/89 (1.00) | 4/4 (1.00) | `rag-context-engine-and-repo-indexing` epic 8, implementation `8c32088` (harvested with explicit range `f9a07b8..8c32088`) — generalizes epic 7's two hardcoded patches into a real gitignore-aware exclusion (`git ls-files --others --ignored --exclude-standard --directory` on the host, since the container has no `git`), plus content-hash-based stale-image detection. **Two real incidents during this epic's own verification, both disclosed:** (1) the original always-`--no-cache` rebuild design failed outright under real network pressure (`DeadlineExceeded` during `pip install`) — corrected to a two-tier design (cached build first, `--no-cache` only as a verified fallback) after live-testing exposed the flaw; (2) a self-inflicted mistake — `git checkout -- docker/rag_cli.py`, run without checking `git status` first, reverted the epic's entire uncommitted diff rather than just a one-line test edit. Caught immediately (the next rebuild failed with a missing-argument error), recovered by reconstructing the exact diff from what had already been reviewed in full the same turn, verified via matching diff-stat and `py_compile`. No data was actually lost, but a real process failure this repo's own git-safety discipline exists to prevent. Verified live: dynamic exclusion catches real content the old hardcoded list never covered (`.claude/worktrees/`, `.DS_Store`, `.claude/settings.local.json`), and the two-tier rebuild correctly skips when unchanged (1.7s) and rebuilds fast when it should (6.2s, reusing cached layers). |
